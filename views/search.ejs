<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="css/styles.css" />
    <title>Search</title>
  </head>
  <body>
    <div class="container">
      <div class="form-container">
        <form action="/search" method="POST">
          <input type="text" name="movieTitle" placeholder="Search" />
          <button type="submit">Search</button>
        </form>
      </div>
      <% if(movieData) { %>
      <div class="result-container">
        <ul class="movie-data-list">
          <li><span>Title:</span><%= movieData.title %></li>
          <li><span>Year:</span><%= movieData.year %></li>
          <li><span>Genres:</span><%= movieData.genres %></li>
          <li><span>Overview:</span><%= movieData.overview %></li>
        </ul>
        <div class="image-container">
          <img
            src="<%= movieData.posterUrl %>"
            alt="<%= movieData.title %> poster" />
        </div>
      </div>
      <% } %>
    </div>
  </body>
</html>
